#include <bits/stdc++.h>
using namespace std;
int n, k, a[1005];
void input()
{
    cin >> n >> k;
    for(int i = 1; i <= k; i++)
    {
        cin >> a[i];
    }
}
void nextGen()
{
    for(int i = k; i >= 1; i--)
    {
        if()
    }
}
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        
    }
}